function fight(){
    atk.addEventListener("click", player-basicAtk);
    esc.addEventListener("click", escaping);
}

function player-basicAtk(){
    var damageRoll = Math.ceil(Math.random() * 5) + player.atk;
    enemyhp -= damageRoll;
}

var escapeCount = 0;
console.log(escapeCount);
function escaping(){
    if (escapeCount == 3) {
        alert("You could escape no longer and have been slain. Game Over!")
    } else {
        escapeCount++;
    }
}